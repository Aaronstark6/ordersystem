# 当前任务

当前任务：
- `STAGE3_CONDITION_POLICY_DESIGN_01`

目标：
- 补齐 Condition Policy 设计。
- 明确 Condition V1 能力范围、控制对象、控制行为和导出边界。

边界：
- 只修改项目文档。
- 不修改主链。
- 不创建 `app/export/policies/` 或实现 ConditionPolicy。
- Condition 不直接生成 ExportOperation。
