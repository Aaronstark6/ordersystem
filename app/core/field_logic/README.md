目录：
app/core/field_logic

职责：
提供字段逻辑（Field Logic）能力。

当前能力：
- 字段候选（FieldCandidate）。
- 字段标准化（Field Normalization）。
- 字段匹配候选（FieldMatchCandidate）。
- 简单字段匹配（Simple Field Matching）。

当前不做：
- 不调用 AI。
- 不生成 DocumentModel。
- 不生成 Workspace。
- 不执行 Export。
